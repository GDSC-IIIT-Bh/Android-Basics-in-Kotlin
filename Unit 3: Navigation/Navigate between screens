1. Which of the following is false about collections and higher order functions in Kotlin?

a)Lists, maps, and sets can all use higher order functions.

b)Lists are unordered, while maps and sets are ordered data types.

c)Like the elements in a set, the keys in a map must be unique. However, multiple keys can map to the same value.

d)Higher order functions such as map and filter can take lambda functions as parameters.


Ans : 


2. Given the following code, what is the result of oneWordCities[1]?

val cities = listOf("Jeddah", "Bengaluru", "Shenzhen", "Abu Dhabi", "Mountain View", "Tripoli", "Bengaluru", "Lima", "Mandalay", "Tripoli")
val oneWordCities = cities.toSet().toList().filter { !it.contains(" ")}.sorted()

a)Tripoli

b)Abu Dhabi

c)Jeddah

d)Bengaluru


Ans : 


3. Fill-in-the-blanks
Enter one or more words to complete the sentence.

A(n) 
Fill in the blank...
 is a piece of data passed between

Ans : 


4. If you open an app, and then leave the app using the back button, in which order were the following activity lifecycle methods called?

onStart(), onCreate(), onDestroy(), onStop()

onDestroy(), onStart(), onCreate(), onStop()

onCreate(), onStart(), onStop(), onDestroy()

onStart(), onCreate(), onStop(), onDestroy()


Ans : 


5. Which activity lifecycle method would be called if a dialog appears onscreen, partially obscuring an activity?

a)onPause() because the activity is still displayed, but no longer has focus.

b)onStop() because the activity does not need to respond to user input while the dialog is onscreen.

c)onResume() because the activity needed to respond to user input to display the dialog.

d)onDestroy() because the activity does not need to exist so long as it doesn’t have focus.


Ans : 


6. Which of the following is true about the lifecycle of a single activity?
Choose as many answers as you see fit.


a)onStart() can be called multiple times, while onCreate() can only be called once.

b)onStop() can be called multiple times, while onPause() can only be called once.

c)onDestroy() is called when the app enters the background.

d)onResume() is called when the activity gains focus.


Ans : 


7. Which of the following is false about intents?

a)Both implicit and explicit intents allow your app to launch another activity.

b)Explicit intents require you to specify the class of the activity you want to show.

c)Intents are performed using the startActivity() method.

d)An implicit intent always results in the system asking the user which app to open.


Ans : 


8. An activity contains the following code in onCreate(). What will happen when this code is executed, if the intent property is null?

val message = intent.extras?.getString("message"
).toString()

a)The app will crash because it attempted to access the extras property on a null object.

b)The app will crash because it attempted to access a null object.

c)The app will not crash because the extras property is accessed unsafely using ?.

d)The app will not crash because the extras property is accessed safely using ?.


Ans :



9.Which of the following tasks can be performed in onCreate()?
Choose as many answers as you see fit.


a)Configuring views, such as setting the layout manager of a recycler view.

b)Determining the items to be shown in the options menu.

c)Setting the onClickListener for items in the options menu.

d)Getting extras from the intent that launched the activity.

Ans:


10.In which method should you handle what happens when a button in the app bar is pressed?

a)onCreateOptionsMenu()

b)openOptionsMenu()

c)onOptionsItemSelected()

d)onPrepareOptionsMenu()

Ans:















